# 躺平城市数据管理指南

本文档介绍如何清除旧的躺平城市数据并导入新数据。

## 快速开始

### 方式一：清除旧数据并导入新数据（推荐）

这是最常用的方式，会完整地替换所有躺平榜数据：

```bash
node clear-and-import-data.js
```

### 方式二：仅清除数据

如果你只想清除旧数据，不导入新数据：

```bash
node clear-and-import-data.js --only-clear
```

### 方式三：仅导入新数据

如果数据库是空的，或者你只想增量导入新数据：

```bash
node clear-and-import-data.js --skip-clear
```

## 数据文件

新数据应该放在以下位置：

```
data/layflat-cities-ranking.json
```

数据格式示例：

```json
[
  {
    "rank": 1,
    "name": "鞍山市",
    "province": "辽宁",
    "district": "铁西区、铁山区",
    "location": "辽东半岛中部",
    "avg_rent": 300,
    "house_price": 1000,
    "layflat_level": "金牌（人少）",
    "medical": "普通",
    "suitable_for": "地理环境好的移民人",
    "climate_desc": "四季分明",
    "evaluation": "一手老一手住在大小区，温泉城，路况不太好了下来了",
    "notes": "曾经是钢产业很强大，人气减少",
    "city_tier": "tier3"
  }
]
```

## 脚本功能说明

### 清除操作会删除的数据

脚本会删除所有 `list_type='china_layflat'` 的城市及其关联数据：

- ✅ 城市基本信息（cities 表）
- ✅ 城市维度数据（city_dimensions 表）
- ✅ 城市标签（city_tags 表）
- ✅ 城市图片（city_images 表）
- ✅ 城市评论（reviews 表）
- ✅ 城市收藏（favorites 表）

### 导入操作会创建的数据

- ✅ 城市基本信息
- ✅ 自动计算的躺平评分
- ✅ 7个维度的评分（租金成本、慢节奏、医疗设施等）
- ✅ 自动生成的标签（看海小城、超低房租、极度安静等）

### 自动评分逻辑

**躺平评分** (基础分7.0)：
- 租金 < 300元：+1.5分
- 租金 300-400元：+1.2分
- 租金 400-500元：+0.8分
- 租金 500-600元：+0.5分
- 躺平等级包含"金牌"：+0.8分
- 躺平等级包含"人少"：+0.5分
- 躺平等级包含"舒适"：+0.6分
- 医疗设施"普通"：+0.3分
- 医疗设施"良好"：+0.5分

**维度评分**：
- 租金成本：根据平均租金自动计算（租金越低分数越高）
- 慢节奏：根据躺平等级判断
- 医疗设施：根据医疗字段计算
- 自然环境：根据评价内容判断（包含"森林"、"山"等关键词）
- 人口密度：根据躺平等级判断
- 物价指数：基于租金数据
- 数字设施：根据城市等级判断

### 自动标签生成规则

| 标签 | 触发条件 |
|------|----------|
| 💰 超低房租 | 平均租金 < 400元 |
| 🌲 东北小城 | 省份为黑龙江、吉林、辽宁 |
| ❄️ 寒冷气候 | 东北地区或气候描述包含"寒" |
| ☀️ 温暖气候 | 省份为云南、海南、广东、福建 |
| 👴 养老天堂 | 评价包含"养老"、"养生"、"康养" |
| 🏖️ 看海小城 | 评价包含"海"、"沿海" |
| 🤫 极度安静 | 躺平等级包含"人少" |

## 常见问题

### Q: 如何更新部分城市数据？

A: 脚本会自动跳过已存在的城市（基于城市名称和省份判断）。如果你想更新现有城市，需要先清除数据。

### Q: 导入失败怎么办？

A: 脚本会显示详细的错误信息，包括哪个城市导入失败以及失败原因。你可以：
1. 检查数据文件格式是否正确
2. 查看失败城市的具体错误信息
3. 修复数据后重新运行脚本

### Q: 可以导入其他榜单的数据吗？

A: 这个脚本专门用于躺平榜（`china_layflat`）。如果要导入其他榜单，请使用对应的导入脚本。

### Q: 如何验证导入是否成功？

A: 可以使用验证脚本：

```bash
node verify-import.js
```

## 其他导入脚本

项目中还有其他导入脚本：

- `import-data.js` - 早期的简单导入脚本（硬编码少量城市）
- `import-layflat-ranking.js` - 仅导入数据，不包含清除功能
- `clear-and-import-data.js` - **推荐使用**，完整的清除+导入方案

## 技术细节

### 数据库表结构

主要涉及以下表：

```sql
-- 城市表
cities (id, name, province, slogan, avg_rent, layflat_score, ...)

-- 维度表
city_dimensions (city_id, rent_cost, slow_pace, medical_access, ...)

-- 标签表
city_tags (city_id, tag_key, tag_name, tag_icon, ...)
```

### 关联数据处理

脚本使用外键级联删除（`ON DELETE CASCADE`），确保删除城市时自动清理关联数据。

## 示例输出

```
========================================
  躺平榜城市数据管理工具
========================================

✓ 数据库连接成功

🗑️  开始清除躺平榜城市数据...

📊 找到 241 个躺平榜城市数据

✓ 已删除城市标签数据
✓ 已删除城市维度数据
✓ 已删除城市图片数据
✓ 已删除城市评论数据
✓ 已删除城市收藏数据
✓ 已删除城市基本数据

✅ 清除完成！共删除 241 个城市及其关联数据

📥 开始导入躺平榜城市数据...

📊 共有 241 个城市待导入

✓ 导入成功: 鞍山市 (辽宁) - 评分: 8.8 - 排名: #1
✓ 导入成功: 攀枝花 (四川) - 评分: 8.5 - 排名: #2
...

========================================
✅ 导入完成！
   成功: 241 个
   跳过: 0 个
   失败: 0 个
   总计: 241 个
========================================

🎉 操作完成！
```
